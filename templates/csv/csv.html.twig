<!DOCTYPE html>
<html lang="en">
<head>
<title></title>
{% for url in css %}
<link href="{{ url }}" rel="stylesheet">
{% endfor %}
</head>
<body>
<div class="container-fluid">
<article>
<pre>{{ csv|raw }}</pre>
</article>
</div>
{% for url in js %}
<script src="{{ url }}"></script>
{% endfor %}
</body>
</html>
