MyFolder.behaviors.createAccountNextModal = {
    attach: function (context, settings) {
        console.log('|-MyFolder.behaviors.createAccountNextModal.attach()');
        // $('#create-account-next').once('create-account-next').on('click', function (e) {
            
            // this.disabled = true
            // this.innerText = 'Waiting'
            // let $form = $(this).parents('.modal-content').find('form');
            // let inputSysadminName = $form.find('[name=sysadminName]')[0];
            // let inputSysadminPassword = $form.find('[name=sysadminPassword]')[0];
            // inputSysadminName.disabled = true
            // inputSysadminPassword.disabled = true
            // let sysadminName = $form.find('[name=sysadminName]')[0].value;
            // let sysadminPassword = $form.find('[name=sysadminPassword]')[0].value;
            // let url=$form.attr('action');
            // console.log(url);
            // let data = {
                // action: 'sed',
                // name: sysadminName,
                // pass: sysadminPassword
            // }
            // const formData = new FormData();
            // formData.append("name", sysadminName);
            // formData.append("pass", sysadminPassword);
            // let info = {
                // url: url,
                // _pseudoLink: true,
                // options: {
                    // method: "POST",
                    // body: formData
                // }
            // }
            // MyFolder.fetch(info);
            // url = MyFolder.pseudoLink(url);
            // let options = {
                // type: "POST",
                // url: url,
                // 
            // }
            // MyFolder.ajax(context, options);
        // });
    }
}
